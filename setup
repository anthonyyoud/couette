# Compile and setup ready to run in a separate directory
# usage:  source setup <directory name> [compile params]

mkdir $1

#Compile
make clean; make

#Setup dir
mv couette_mod.out $1
cp parameters.f90 $1
cp pbs_run_script.sh $1

cd $1
